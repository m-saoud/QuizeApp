import React, { useState } from 'react'
import QuizData from '../QuizData'

const Quiz = () => {
    const [userAnswers, setUserAnswers] = useState([]);
    const [currentQuestionIndex, setCurrentQuestionIndex] = useState(0);


    const quize =() => {
        setquastion(quastion)
    }



  return (
      <div>
          
    </div>
  )
}

export default Quiz
